streams:
  # Main test stream (Synthetic Pattern via Exec)
  # This runs ffmpeg INSIDE the container to generate video. No internet needed.
  bunny: 
    - exec:ffmpeg -re -f lavfi -i testsrc=size=320x240:rate=15 -c:v libx264 -g 15 -preset ultrafast -tune zerolatency -f rtsp {output}
  cam_27671429:
    - rtsp://admin:Vigil@123@192.168.1.13:554/1/1

api:
  listen: ":1984"

rtsp:
  listen: ":8554"

webrtc:
  listen: ":8555"
  candidates:
    - "192.168.1.8:8555"
